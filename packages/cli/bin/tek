#!/bin/bash
# Wrapper script for tek CLI that suppresses Node.js deprecation warnings
# about punycode module from transitive dependencies
export NODE_NO_WARNINGS=1
SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
exec node "$SCRIPT_DIR/../dist/index.js" "$@"
